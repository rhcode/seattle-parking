ingest {
  timestampFormat = "MM/dd/yyyy hh:mm:ss aa"
}

stream {
  latenessTolerance = 10 // minutes
  slidingWindowSize = 15 // minutes
  slideSize = 5 // minutes
}

sink {
  host = "0.0.0.0"
  port = 9200
  index = seattle-parking-index2
  docType = parking-locations
  fields {
    count = count
    timestamp = time
    location = location
  }
}